#!/usr/bin/env ruby

REPO_LIST = %w(bugmark/iora bugmark/exchange bugmark/bmx_cl_ruby)

REPO_LIST.each do |repo_name|
  `bmx repo create #{repo_name}`
end

REPO_LIST.each do |repo_name|
  `bmx repo sync #{repo_name} --source=github:#{repo_name}`
end
