#!/usr/bin/env ruby

TRACKER_LIST = %w(bugmark/iora bugmark/exchange bugmark/bmx_cl_ruby)

TRACKER_LIST.each do |tracker_name|
  `bmx tracker create #{tracker_name}`
end

TRACKER_LIST.each do |tracker_name|
  `bmx tracker sync #{tracker_name} --source=github:#{tracker_name}`
end
